eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka:8761/eureka/
  instance:
    hostname: eureka

spring:
  cloud:
    stream:
      function:
        defination: authentication
      bindings:
        authentication-out-0:
          destination: authentication
      kafka:
        binder:
          brokers:
            - kafka:9092

management:
  endpoints:
    web:
      exposure:
        include: "*"